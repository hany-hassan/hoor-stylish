# 🚀 دليل نشر Hoor Stylish على Vercel

## خطوات النشر المبسطة:

### 📋 المتطلبات:
- حساب GitHub (مجاني)
- حساب Vercel (مجاني)
- المشروع منشور على GitHub

---

## 🔵 الطريقة 1: النشر المباشر من Vercel Dashboard

### الخطوة 1: رفع المشروع على GitHub
```bash
# في مجلد المشروع
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/YOUR_USERNAME/hoor-stylish.git
git push -u origin main
```

### الخطوة 2: ربط المشروع مع Vercel
1. اذهب إلى [vercel.com](https://vercel.com/new)
2. سجل الدخول بحساب GitHub
3. اختر **Import Git Repository**
4. اختر مستودع `hoor-stylish`
5. **لا تضغط Deploy بعد!** ⚠️

### الخطوة 3: إنشاء قاعدة بيانات Postgres
1. في نفس صفحة Import، اذهب للأسفل
2. اضغط **Create** تحت قسم **Storage**
3. اختر **Postgres**
4. اكتب اسم قاعدة البيانات: `hoor-stylish-db`
5. اختر المنطقة الأقرب لك
6. اضغط **Create**

### الخطوة 4: ربط قاعدة البيانات تلقائياً
- Vercel ستربط قاعدة البيانات تلقائياً بالمشروع ✨
- ستضاف متغيرات البيئة تلقائياً

### الخطوة 5: Deploy
1. الآن اضغط **Deploy**
2. انتظر 2-3 دقائق
3. 🎉 **تم!** الموقع أصبح جاهزاً!

---

## 🔵 الطريقة 2: إذا قمت بالنشر قبل إنشاء قاعدة البيانات

### إنشاء قاعدة البيانات بعد النشر:
1. اذهب إلى Dashboard مشروعك في Vercel
2. اذهب إلى تبويب **Storage**
3. اضغط **Connect Store**
4. اختر **Create New** → **Postgres**
5. اكتب اسم قاعدة البيانات
6. اضغط **Create & Continue**
7. **اختر المشروع** `hoor-stylish` للربط
8. اضغط **Connect**

### تحديث Environment Variables:
Vercel ستضيف المتغيرات تلقائياً، لكن تأكد من وجود:
- `POSTGRES_URL` → تم إضافته تلقائياً كـ `DATABASE_URL`
- `POSTGRES_URL_NON_POOLING` → (اختياري)

### إعادة النشر:
1. اذهب إلى تبويب **Deployments**
2. اضغط على آخر deployment
3. اضغط **⋯** (ثلاث نقاط)
4. اختر **Redeploy**

---

## 📊 إضافة البيانات الأولية

بعد النشر، قاعدة البيانات ستكون فارغة. لإضافة البيانات:

### الطريقة الأولى: من لوحة Vercel
1. اذهب إلى **Storage** → قاعدة البيانات
2. افتح تبويب **Query**
3. شغّل الـ migrations يدوياً

### الطريقة الأفضل: استخدام Prisma Studio
```bash
# من جهازك المحلي
# احصل على DATABASE_URL من Vercel
# ضعه في ملف .env

# شغّل migrations
npx prisma migrate deploy

# أضف البيانات الأولية
npm run db:seed
```

---

## 🔗 الوصول للموقع

بعد النشر، ستحصل على رابط مثل:
```
https://hoor-stylish.vercel.app
```

يمكنك أيضاً:
- ✅ إضافة نطاق خاص (Custom Domain)
- ✅ الحصول على شهادة SSL مجانية
- ✅ تعديل الكود والنشر تلقائياً مع كل push

---

## ⚠️ ملاحظات مهمة

1. **الحساب المجاني في Vercel يشمل:**
   - ✅ Bandwidth غير محدود
   - ✅ 100 GB لكل شهر
   - ✅ Postgres مجاني (256 MB)
   - ✅ Auto HTTPS/SSL

2. **للتطوير المحلي:**
   - أنشئ ملف `.env` في جذر المشروع
   - أضف `DATABASE_URL` من Vercel أو استخدم قاعدة محلية

3. **Environment Variables المطلوبة:**
   - `DATABASE_URL` - Vercel تضيفه تلقائياً ✅

---

## 🐛 حل المشاكل الشائعة

### المشكلة: "Prisma Client not generated"
```bash
# في Vercel، أضف Build Command:
prisma generate && next build
```

### المشكلة: "Cannot connect to database"
- تأكد أن DATABASE_URL موجود في Environment Variables
- أعد deploy المشروع

### المشكلة: "Migration not applied"
- استخدم `npx prisma migrate deploy` من جهازك المحلي
- أو شغّل migrations من Vercel Postgres Query

---

## 📞 الدعم

إذا واجهت أي مشكلة:
1. تحقق من Logs في Vercel Dashboard
2. راجع [Vercel Docs](https://vercel.com/docs)
3. تحقق من [Prisma Docs](https://www.prisma.io/docs)

---

✨ **الآن موقعك يعمل بشكل احترافي على Vercel مع قاعدة بيانات حقيقية!**
