# ⚡ دليل البدء السريع

## النشر على Vercel في 5 دقائق 🚀

### الخطوة 1: رفع المشروع على GitHub
```bash
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/hoor-stylish.git
git push -u origin main
```

### الخطوة 2: النشر على Vercel
1. اذهب إلى: https://vercel.com/new
2. سجل دخول بحساب GitHub
3. اختر مستودع `hoor-stylish`
4. **قبل الضغط على Deploy:**
   - اضغط "Storage" → "Create" → "Postgres"
   - سمّي قاعدة البيانات: `hoor-stylish-db`
   - اضغط "Create"
5. الآن اضغط **Deploy**
6. انتظر 2-3 دقائق ✨

### الخطوة 3: الانتهاء!
موقعك أصبح جاهزاً على: `https://hoor-stylish.vercel.app`

---

## ملاحظات مهمة ⚠️

1. **قاعدة البيانات ستكون فارغة** - ستحتاج لإضافة البيانات الأولية
2. **للمزيد من التفاصيل** - راجع ملف [DEPLOY.md](./DEPLOY.md)
3. **للتطوير المحلي** - راجع ملف [README.md](./README.md)

---

## هل واجهت مشكلة؟ 🐛

### "Build failed"
- تأكد أن قاعدة البيانات تم إنشاؤها وربطها
- تحقق من وجود `DATABASE_URL` في Environment Variables

### "Cannot connect to database"
- أعد deploy المشروع بعد إنشاء قاعدة البيانات

### "Prisma error"
- Vercel تقوم بتوليد Prisma Client تلقائياً
- إذا استمرت المشكلة، تأكد من `vercel.json`

---

✅ **كل شيء يعمل الآن!**
